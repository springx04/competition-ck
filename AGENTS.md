# 项目指令

## 默认服务器

- 本项目需要连接服务器时，默认使用 SSH 别名 `e-question-server`。
- 该别名由本机 OpenSSH 配置映射到 `root@183.147.142.40:31317`。使用 `ssh e-question-server` 或对应的 `scp` 命令。
- 服务器凭据只由本机 SSH 配置和认证提示处理。不要把密码或私钥写入仓库；需要密码交互时，让用户在终端输入。

## 服务器操作授权

- 用户已授权本项目任务通过上述服务器进行 SSH 登录、读取项目文件、修改项目代码和运行项目命令；常规项目操作无需重复询问。
- 认证凭据不得写入仓库。删除数据、不可逆变更、正式发布等高风险操作仍按项目要求处理。
