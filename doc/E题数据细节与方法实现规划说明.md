# E题数据细节与方法实现规划说明

核查日期：2026-09-23。依据为本目录题目 DOCX 与解压后的 `E题数据`，未引入外部数据或假定公开版 MOSEI 的默认配置。本文用于方法和实现规划，不包含模型训练及预测结果。

文中“题目要求”指 DOCX 的明确要求，“实测”指实际文件检查结果，“建议”是可选实现方案。尤其注意：**附件3、附件4的实际结构并不完全等同于题目对附件2的概述，不能只按题目中的字段表写一个通用读取器。**

## 1 先回答最影响规划的问题

| 规划问题 | 结论 |
| --- | --- |
| Q1 输出是什么 | 附件1全部100条样本的自提取三模态时序特征，以及样本标识、有效长度、填充规则、时序映射、提取配置等；不是100个情感预测值 |
| Q1 特征必须是多少维 | 题目没有固定 Q1 的 `D_text`、`D_audio`、`D_vision`，也没有强制 Q1 必须长50；维度和对齐粒度需自行定义并说明 |
| Q1 输出维度必须等于 Q2 输入吗 | **题目不强制，但论文建议主动衔接。** 优先统一时序表示、掩码和时间映射；能确认特征定义一致时再统一原始维度。Q2、Q3仍在附件2指定训练集学习参数 |
| 如果希望 Q1 接入 Q2 呢 | 必须兼容特征含义、编码器、归一化、时序位置、掩码和维度；仅凑成 `768/74/35` 不能证明兼容。另须遵守训练集使用范围，不能把100条全部加入 Q2 训练 |
| Q2 真正输入是什么 | 附件2训练/验证数据，最终预测附件3；选定对齐或未对齐版，并解决附件3的字段缺失问题 |
| Q3 真正输入是什么 | 附件2训练/验证数据，最终预测附件4；附件4视频用于解释结果回看、证据定位 |
| 情感任务是几分类 | 三分类：负向、中性、正向；同时预测 `[-3,3]` 范围的连续强度。不是六类情绪分类，也不是只有正负两类 |
| 最终需要多少条专项预测 | 选择一套版本时，Q2 为30条，Q3 为20条；两套版本不能当作两倍独立样本 |
| Q2、Q3 是否必须同一模型 | 题目未要求架构、参数相同；**本文建议 Q3 继承 Q2 的预测主干并增加解释模块**，让鲁棒预测与证据分析围绕同一决策过程展开，分别完成两类专项任务 |
| 是否有统一规定的 CSV 列名 | 当前题目和解压目录未提供逐列模板；只要求 Q2 预测 CSV、Q3 预测与解释 CSV。本文的列名是建议，不是官方模板 |

题目规定的数据使用关系是：

```text
附件1 原视频 + 转写 + 标签 ──> Q1 特征提取与对齐 ──> 100条自生成特征与核验材料

附件2 train / valid ──> Q2 鲁棒模型 ──> 附件3 所选版本 ──> 30条预测
附件2 train / valid ──> Q3 可解释模型 ──> 附件4 所选版本 ──> 20条预测与解释
                                                            └─ 配套视频回看
```

论文与实现建议采用递进主线：**Q1 建立可追溯表示 → Q2 在该表示框架下实现缺失鲁棒预测 → Q3 解释预测依据，并通过证据回溯和干预实验检验表示与鲁棒机制。** 上图表示各问的数据来源，不能据此将三问写成互不关联的三个方案。具体衔接见第10节。

这里的闭环由共同的表示规则、可继承的预测模型和验证实验构成。Q1 的100条特征仍是独立要求的产物，不自动成为 Q2/Q3 的训练数据；附件2缺少原始时间映射的部分也不能假称已经由 Q1 补齐。

## 2 本地文件清单和样本粒度

数据根目录为 `C:\Users\30738\Desktop\sm\中文题目\E题\E题数据`。

```text
E题数据/
├─ 附件1-数据集原始多模态样本/
│  └─ MOSEI数据集部分原始视频-100条/
│     ├─ label-100.xlsx
│     └─ <video_id>/<clip_id>.mp4
├─ 附件2-数据集特征文件/
│  ├─ aligned_50.pkl
│  ├─ unaligned_50.pkl
│  └─ label.xlsx
├─ 附件3-模态缺失特征样本/
│  ├─ 对齐版本/附件3_01.pkl ... 附件3_30.pkl
│  └─ 未对齐版本/附件3_未对齐版本_01.pkl ... 附件3_未对齐版本_30.pkl
└─ 附件4-可解释专项视频样本与特征文件/
   └─ 附件4-可解释专项视频样本与特征文件/
      ├─ 对齐版本/
      │  ├─ 01.pkl ... 20.pkl
      │  └─ videos/01.mp4 ... 20.mp4
      └─ 未对齐版本/
         ├─ 01.pkl ... 20.pkl
         └─ videos/01.mp4 ... 20.mp4
```

附件4确实有一层重复目录名，视频在每套版本的 `videos` 子目录中，不与 PKL 直接同层。

| 附件 | 实际规模 | 标签和用途 |
| --- | --- | --- |
| 附件1 | 37个视频来源文件夹、100个 MP4、1个 XLSX | 有转写与标签；Q1原始素材 |
| 附件2 | 两个 PKL，各4850条；1个 XLSX | 有标签、固定 train/valid/test；Q2/Q3建模 |
| 附件3 | 每版本30个单样本 PKL，共60个文件 | 无标签，无原始视频；Q2专项推理 |
| 附件4 | 每版本20个单样本 PKL、20个 MP4 | 无标签；Q3专项推理与解释回看 |

附件4两套版本同编号的视频已逐字节比较，20对全部相同，因此是20个视频样本、40份物理视频文件。附件2两版本的各划分 ID 顺序完全一致，也不是9700条独立样本。附件3没有内部 ID，不应仅靠编号假定其两版本的扰动位置完全一致。

附件2文件大小约为：对齐版993.84 MB，未对齐版2897.00 MB，均按十进制 MB 计。目录中的 `.DS_Store` 不参与建模；已有 ZIP 是数据包，读取解压目录即可，不需要重复统计。

## 3 附件1 原始素材与 Q1 输入

### 3.1 样本标识和标签表

一条样本是一个视频片段，主键由 `(video_id, clip_id)` 组成。不同文件夹可以出现相同 `clip_id`，不能只用文件名作为全局主键。

`label-100.xlsx` 主数据工作表有100条数据，字段如下：

| 字段 | 实际内容 | 使用方式 |
| --- | --- | --- |
| `video_id` | 视频来源标识字符串 | 对应子文件夹 |
| `clip_id` | 片段编号 | 对应 MP4 文件名，不应误写为 `13.0.mp4` |
| `text` | 英文转写 | 可直接作为文本模态输入，并用于强制对齐 |
| `label` | 连续情感强度 | 片段级标签，不是逐词或逐帧标签 |
| `annotation` | `Negative/Neutral/Positive` | 片段级三分类标签 |

实测100个视频与主表100个主键一一对应。类别分布为负向18、中性25、正向57。工作簿还有一个核查说明工作表，不要把它作为另一批样本读入。

MP4容器记录的时长为2.648～34.567秒，与题目一致。视频尺寸并不统一，不能把题目示意图中的分辨率、帧率或采样率当作全部视频的参数。正式提取时应逐视频读取真实音视频流信息，分别记录重采样、缩放和抽帧规则。

### 3.2 Q1 有哪些已经提供的信息

已经提供视频、英文转写和片段级情感标签；**没有独立音频文件、逐词时间戳、抽帧时间表或预先生成的 Q1 特征**。音频需要从 MP4 分离，词与声音的时间对应需要自己建立。

Q1 不必先做完整 ASR 才能得到文本；题目已有转写，可以将已有文本与音频对齐。若另外使用 ASR，应说明它与提供转写的关系及错误处理方法。

附件1与附件2按完整样本 ID 实测重合18条，其中附件2 train 中11条、test 中7条、valid 中0条，其余82条未出现在当前附件2中。**不能把附件1理解成“附件2的100条训练子集”，也不能把其标签整体加入 Q2/Q3 训练。**

### 3.3 Q1 建议的输出形式与维度

题目要求可复现、可核验的时序特征，没有规定必须使用 PKL。可以每样本一个 NPZ，加一个 JSON 元数据文件；也可以集中存储，只要提供清晰读取方法。

下面是一种可实施的设计，维度是设计参数，不是题目规定：

| 输出字段 | 单样本形状或类型 | 用途 |
| --- | --- | --- |
| `sample_id` | 字符串 | 例如 `video_id$_$clip_id` |
| `text_features` | `(L, D_t)` | 对齐后的文本时序特征 |
| `audio_features` | `(L, D_a)` | 对齐后的声音时序特征 |
| `vision_features` | `(L, D_v)` | 对齐后的视觉时序特征 |
| `time_intervals` | `(L, 2)`，单位秒 | 每个位置在当前 MP4 片段内的起止时刻 |
| `valid_mask` | `(L,)` | 哪些位置属于实际序列、哪些是填充 |
| `observed_mask` | `(L, 3)` | 有效位置上每个模态是否确实提取成功 |
| `valid_length` | 整数 | 有效位置数；如有内部空缺仍须结合掩码 |
| `token_or_word_spans` | 变长记录 | 文本字符区间、词、子词与对齐位置的对应 |
| `source_frame_indices` | 变长记录 | 视觉特征对应的原视频帧或帧区间 |
| `metadata` | JSON对象 | 原视频路径、时长、提取器版本、参数、截断与失败记录 |

这里 `L` 是共同时间轴的长度。可以按词/子词对齐，也可以按固定时间窗对齐；两者的时间含义不同，需要在论文中说明。三种模态在相同 `L` 上对应同一时间区间，但 `D_t`、`D_a`、`D_v` 不必相同。

若选择最多50个位置，则100个样本可保存为 `(100,50,D_t)`、`(100,50,D_a)`、`(100,50,D_v)`；若保留变长序列，则每样本保存自己的 `L_i`，训练组批时再填充。题目没有要求固定50，不能为了凑50而无记录地丢弃较长片段。

为增强三问连贯性，建议先确定后两问的输入路线，再设计 Q1 输出：尽量复用文本编码器、位置编号、掩码定义和时间映射记录方式，并保留提取后的原始特征。若能确认提取流程一致，可选 `D_t=768, D_a=74, D_v=35`，使形状与附件2对齐版一致。

若无法确认音频74维、视觉35维的完整提取配置，应保留 Q1 合理的自选特征维度，通过统一的数据组织和模态投影模块设计衔接后续方法。投影到同一个隐藏维度只解决计算接口，不能证明两套特征语义相同，也不能据此直接共用已训练权重。论文需要如实说明 Q1 验证原始信号处理流程，Q2/Q3使用官方预提取特征训练的衔接边界。

Q1 最终还需要100条样本的汇总表，至少包括样本编号、各模态维度、原始有效时长、对齐粒度、有效长度和处理状态。论文中需展示至少1个典型样本的文本—音频时段—视频帧段—特征位置对应关系，并提交全部100条特征。失败样本也要保留并说明处理，不可悄悄删除。

## 4 附件2 实际字段 形状 标签与划分

### 4.1 字典层级和样本数

两个文件均为：

```python
data = pickle.load(open(path, "rb"))
split = data["train"]                  # 或 valid / test
sample_id = split["id"][j]
audio = split["audio"][j]              # 字段在前，样本索引在后
```

不能写成 `data['train'][j]['audio']`。

| 划分 | 样本数 | 负向 0 | 中性 1 | 正向 2 |
| --- | ---: | ---: | ---: | ---: |
| train | 3395 | 967 | 758 | 1670 |
| valid | 728 | 206 | 184 | 338 |
| test | 727 | 207 | 158 | 362 |
| 合计 | 4850 | 1380 | 1100 | 2370 |

实测划分之间样本 ID 不重合，`video_id` 也不重合。这一来源视频隔离值得保留，不建议把4850条打乱后重新随机划分。两个版本同一划分中的 ID 顺序相同。

`label.xlsx` 主表有4850条记录，字段是 `video_id, clip_id, text, label, annotation, mode`。其 `mode` 与 PKL 划分一致，连续标签也一致。连接 XLSX 与 PKL 应使用完整主键，不依赖 Excel 行号或目录遍历顺序。

### 4.2 每个划分的实际字段

下表中 `N` 为当前划分的样本数。

| 字段 | 对齐版形状 | 未对齐版形状 | 实际类型 |
| --- | --- | --- | --- |
| `id` | 长度 N | 长度 N | Python 字符串列表 |
| `raw_text` | `(N,)` | `(N,)` | NumPy Unicode 数组 |
| `text` | `(N,50,768)` | `(N,50,768)` | `float32` |
| `text_bert` | `(N,3,50)` | `(N,3,50)` | `int64` |
| `audio` | `(N,50,74)` | `(N,500,74)` | `float64` |
| `vision` | `(N,50,35)` | `(N,500,35)` | `float64` |
| `classification_labels` | `(N,)` | `(N,)` | `float64`，取值0、1、2 |
| `regression_labels` | `(N,)` | `(N,)` | `float64`，范围 `[-3,3]` |
| `audio_lengths` | 不存在 | 长度 N | Python 整数列表 |
| `vision_lengths` | 不存在 | 长度 N | Python 整数列表，有下述异常 |
| `annotations` | **不存在** | **不存在** | 文字标签在 Excel 的 `annotation` 列中 |

分类标签映射已用全部4850条连续标签核对：`0=Negative`、`1=Neutral`、`2=Positive`；与连续值的负、零、正完全一致。读取训练目标时，应把分类标签转成整数类别，连续标签和连续特征通常转为 `float32`。

### 4.3 如何理解各个维度

- `50/500` 是序列位置上限，不是特征维数，不是秒数，也不是样本数。
- `768/74/35` 是各位置的特征维度，不是类别数。
- `text_bert` 中的3是三路输入：词元 ID、注意力掩码、分段 ID；不是三类情感，也不是三种文本特征。
- `text` 是已经编码的连续表示；`text_bert` 需要经过兼容的文本编码器。它们是同一文本模态的不同入口，不应当作第四个模态。
- 对齐版可在同一位置拼接三模态，最后一维为 `768+74+35=877`，得到 `(B,50,877)`；这只是可选融合形式。
- 未对齐版不能直接沿特征维拼接 `(B,50,768)` 和 `(B,500,74)`。应先分别编码/池化、显式重采样，或采用能处理独立序列长度的交互模块。
- 即使两序列都长500，也不代表它们的第 `t` 行对应同一秒；未对齐音频和视觉保留独立时序。

### 4.4 标签和评估边界

题目定义为负值对应负向、零对应中性、正值对应正向。中性不能并入正类。Q2、Q3都需要每片段一个极性结果和一个强度结果，没有提供逐词情感、逐帧情感或人工证据位置标签。

模型可以采用分类头与回归头，也可以从回归结果生成极性。若采用后者，浮点预测恰好等于零的概率很低，因此中性区间/决策规则需要明确，并且只能在验证集选定；不能把这种预测决策规则误写成对真实标签的重新定义。

题目指定 Accuracy、F1、MAE、Pearson。F1 的平均方式没有明确说明，建议同时报告 macro-F1 与 weighted-F1，并标明口径；Pearson 在常量预测时无定义，应如实处理。附件2 `test` 有标签，可在方案固定后作为补充留出评估，不能用于调参；它不替代附件3、附件4的专项提交。

## 5 零值 掩码与长度的实测问题

### 5.1 零值不等同于人工缺失

附件2和附件4没有被定义为随机缺失专项数据，但其特征仍有大量零行。零可能来自尾部填充、特殊词元占位、原始视觉提取无有效结果，或专项数据中的人工遮蔽。应区分：

1. **结构有效掩码**：哪些序列位置属于真实输入范围。
2. **模态观测掩码**：这些位置的某个模态是否可用。
3. **人工扰动掩码**：训练/验证实验中，自己选择遮蔽了哪些位置。

对缺失率做分析时，分母应是原本有效的位置，不能用固定50或500把填充也算进去。专项文件没有提供真实扰动掩码时，不能从所有零行精确反推出人工缺失率。

### 5.2 文本的填充不能从 `text == 0` 判断

实测附件2所有划分中，`text` 的50个位置均没有全零行，包含注意力掩码为0的位置。附件4也是如此。因此必须使用 `text_bert[:,1,:]` 等实际有效性信息进行池化/注意力控制，不能靠连续文本向量是否为零判断 padding。

附件2文本注意力掩码有效计数的最小值/中位数/最大值分别为：train `3/22/50`，valid `3/23.5/50`，test `4/23/50`。该计数包括特殊词元，不能直接解释成英文单词数；达到50也不自动证明原文本恰好只有50个词元，可能存在截断。

对齐音视频中可见首位置及尾部零行，示例与文本特殊词元位置相邻。题目没有完整提供词—子词—音视频位置的原始映射，解释时还需要核验，不能把全部零行当作随机缺失。

### 5.3 视觉全零样本确实存在

| 数据范围 | train | valid | test |
| --- | ---: | ---: | ---: |
| 附件2对齐版视觉全零样本数 | 110 | 15 | 28 |
| 附件2未对齐版视觉全零样本数 | 30 | 0 | 8 |

附件4对齐版 `13.pkl` 的整个 `(50,35)` 视觉矩阵也全零。因此，“三模态完整”是任务场景描述，并不保证每个实际特征矩阵都包含非零有效信号。模型应保留该样本、记录视觉不可用状态，并避免将视觉分支输出的偏置误解释为视觉证据。

附件2、3、4已检查的所有连续特征数组均未发现 NaN 或 Inf；这不意味着零值和长度就没有问题。

### 5.4 未对齐版 `vision_lengths` 不能直接当作可靠截断边界

实测附件2中，在 `vision_lengths` 指定长度之后仍出现非零视觉行的样本数为：

| 划分 | 冲突样本数 |
| --- | ---: |
| train | 618 |
| valid | 141 |
| test | 131 |

例如 train 的 `-HwX2H8Z4hY$_$3`，`vision_lengths=1`，但最后一个非零视觉位置为84（从0计数），全序列有14个非零视觉位置。直接用 `vision[:1]` 会丢弃实际数据。

附件4未对齐版也有同类情况：`13.pkl` 的 `vision_lengths=1`，但非零视觉行有17个、最后位置17；`16.pkl` 的 `vision_lengths=1`，但非零行有40个、最后位置43。附件2的 `audio_lengths` 则与音频非零前缀一致，本次未发现长度之后仍有非零音频行。

建议保留原始长度字段，同时记录由非零支撑范围推定的长度和观测掩码。发生冲突时，不能静默按原字段裁剪；可以在训练/验证中比较保留完整存储序列并使用掩码的方案。非零推定长度也只是推断，不能恢复真实时间戳或区分末尾缺失与填充。论文需明确使用的规则及其局限。

## 6 附件3 Q2 专项输入存在的接口差异

### 6.1 对齐版30个文件

全部30个文件均只有下列内容：

```text
{
  'test': {
    'text_bert': float32 (1, 3, 50),
    'vision':    float32 (1, 50, 35),
    'audio':     float32 (1, 50, 74)
  }
}
```

**没有 `text`、`raw_text`、`id`、标签或长度字段。** 第一维1已经是样本维，不要再无条件加一维。样本结果标识需保留文件名，例如 `附件3_01`。

这里 `text_bert` 虽然存成 `float32`，本次核查其所有值均为整数值，可按三路输入语义转成整数。不要把 `(3,50)` 当成 `(50,768)` 直接输入连续文本分支。

规划含义：若训练只用附件2的预计算 `text`，到附件3会缺少同类型输入。可行路线是从开始就让 Q2 的 train、valid、专项推理统一走 `text_bert → 同一文本编码器`。若要复用附件2的 `text`，必须先明确并验证其原始编码器/词表/层输出等配置；当前文件不能仅凭768维确定这些配置。使用不同编码器生成同为768维的向量，仍可能产生明显输入分布差异。

本次核查，30个对齐版 `text_bert` 中没有出现“非零列范围内部三路同时全零”的洞。因此不能声称本版本一定含可直接观察到的文本内部随机遮蔽；该检查也不能证明文本语义从未受损。Q2 的文本缺失鲁棒性仍应通过自己在训练/验证上构造的实验说明。

### 6.2 未对齐版30个文件

全部30个文件均为：

```text
{
  'test': {
    'raw_text': Unicode array (1,),
    'vision':   float64 (1, 500, 35),
    'audio':    float64 (1, 500, 74)
  }
}
```

**没有 `text`、`text_bert`、`id`、`audio_lengths`、`vision_lengths` 或标签。** 文本需要自行分词和编码，且训练/验证也必须使用一致的分词器、长度限制和编码器。

音视频的尾部零区间无法唯一判断是填充还是末尾缺失；若推定长度，应记录为推定值，保留原始500位置的顺序。不要删除中间零行后将序列压紧，否则会改变缺失位置与时间关系。

以未对齐版01号为例，音频在位置6、17、38～39出现内部零行，并在后部存在长零区间；这说明仅保存一个长度值不够表达局部不可用情况。

### 6.3 对 Q2 方法设计的直接要求

- 模型需要应对局部连续片段缺失，不能只做“整模态丢弃”实验后声称完成全部任务。
- 在附件2 train 上构造局部遮蔽用于训练；在 valid 上按模态类型、位置、时长/比例分组评估。训练和验证的随机样本/扰动实现应可复现。
- 人工遮蔽前保留原始有效性信息，避免把 padding 当成观测内容；全零视觉原样本与新增扰动分开统计。
- 如果模拟文本缺失，应在文本编码前遮蔽对应输入，或明确所模拟的是“编码后特征缺失”。先用完整文本编码、再把局部向量归零，其他上下文向量仍可能携带被遮蔽文本的信息。
- 标准化参数只在 train 的适当有效观测上估计；标准化后再应用缺失掩码，避免零占位经减均值后变成伪观测。
- 不应通过匹配附件2的文本/特征找回附件3原样本，再补回缺失信息或读取其标签；专项数据按题意只用于最终推理。

## 7 附件4 Q3 输入与证据定位

### 7.1 字典层级与形状

附件4每个 PKL **直接保存一条样本，没有 `test` 外层，也没有样本维1**。

| 字段 | 对齐版 | 未对齐版 |
| --- | --- | --- |
| `id` | 字符串 `01`～`20` | 同左 |
| `raw_text` | 单个英文字符串 | 同左 |
| `text` | `(50,768)` float32 | 同左 |
| `text_bert` | `(3,50)` int64 | 同左 |
| `audio` | `(50,74)` float64 | `(500,74)` float64 |
| `vision` | `(50,35)` float64 | `(500,35)` float64 |
| `audio_lengths` | 无 | 单个整数 |
| `vision_lengths` | 无 | 单个整数，13号和16号有上述冲突 |

两版本共40个 PKL 的内部 `id` 均与文件名一致，都能连接到本版本 `videos/<id>.mp4`。组批时再添加样本维或堆叠为 `(B,...)`。两位字符串应保留前导零。

附件4视频容器时长为3.700～24.134秒。PKL 没有原始 MOSEI 的 `video_id$_$clip_id`，不要将 `01` 误认为附件2的原始样本标识。

### 7.2 有视频不代表已有特征位置到秒的映射

当前 PKL 中未提供逐词时间戳、特征帧采样时刻、音视频特征提取窗口、token到时间的映射或人工证据标签。题目希望证据可以回到原始素材，但这条映射需要在实现中补建或核验。

Q3 的解释至少分为三层：

| 层次 | 可保存的内容 | 是否可直接从现有 PKL 得到定位关系 |
| --- | --- | --- |
| 模态作用 | 文本/音频/视觉的重要性及主要参考模态 | 模型计算，无官方解释真值 |
| 特征位置 | 某模态第几个有效位置、连续区间及其重要性 | 可以在现有特征序列上计算 |
| 原始证据 | 英文片段、音频起止秒、视频帧/时间区间 | 需要额外映射和回看验证 |

文本可以通过兼容分词器重建字符区间和词元对应；应核对分词结果与存储的 `text_bert`，不能默认第 `t` 个英文单词就是第 `t` 个特征位置。

音视频若没有采样率和提取时间信息，不能直接使用 `t/50 * 视频时长` 宣称精确定位，尤其对齐版可能按文本位置组织、未对齐版可能截断且视觉长度存在异常。按时长均匀映射只能标注为近似方法并回看核验。较可靠的方案是结合提供的转写与视频建立词级时间对齐，并记录词/子词/特征位置映射；未能验证的对应关系应明确保留不确定性。

重新分析视频可以帮助建立证据映射，但若重新提取一套不同的输入特征并替代专项特征，需要重新考虑与附件2训练输入的兼容性，不能只在专项推理时更换输入空间。

### 7.3 解释输出不止一张注意力图

Q3 要求每条样本给出预测、主要参考模态、三模态作用程度以及关键证据位置。可以定义归一化非负权重 `(w_t,w_a,w_v)`，也可以提供带符号的贡献值；前者表示相对重要程度，后者才可能区分推向正面或负面的方向，二者不能混称。

注意力权重可以作为可视化对象，但不能仅凭权重大小就宣称因果贡献。建议在附件2验证集上做删除关键片段与删除对照片段的输出变化对比，说明解释与模型决策的关系。附件4没有解释真值，不能报告“证据定位准确率”而不说明参考标准。

对视觉全零的附件4对齐版13号，不能从这个全零特征矩阵推导出可信的视觉关键帧；应标记证据不可用，并解释模型主要依赖的其他模态。

## 8 Q2 和 Q3 的建议输入输出维度

以下是实现接口建议，不是新增题目规定。

### 8.1 输入侧

| 内容 | 对齐版批输入 | 未对齐版批输入 |
| --- | --- | --- |
| 编码后的文本 | `(B,50,768)`，若自行编码也可用自定维度 `D_t` | 同左 |
| 音频 | `(B,50,74)` | `(B,500,74)` |
| 视觉 | `(B,50,35)` | `(B,500,35)` |
| 文本有效/观测掩码 | `(B,50)` | `(B,50)` |
| 音频、视觉观测掩码 | 分别 `(B,50)` | 分别 `(B,500)` |
| 分类真值 | `(B,)` 整数，仅有标签划分 | 同左 |
| 回归真值 | `(B,)` float32，仅有标签划分 | 同左 |

模态编码后可以分别投影到同一隐藏维度 `d`，例如得到 `(B,T_m,d)`，再做融合。隐藏维度 `d` 是模型参数，不要求等于原始768、74或35。时间长度相同和特征维度相同是两件事。为形成递进方案，建议 Q2、Q3共用同一文本编码路线、模态投影、掩码处理和预测主干；Q3增加解释输出，保留可复用的 Q2 参数。Q1复用这些表示规则，但不同特征空间的投影权重不能无依据地直接迁移。

### 8.2 输出侧

| 输出 | Q2 | Q3 |
| --- | --- | --- |
| 三分类 logits/概率 | `(B,3)` | `(B,3)` |
| 最终类别 | `(B,)` 或 B 个英文标签 | 同左 |
| 情感强度 | `(B,)`，或内部 `(B,1)` 后展平 | 同左 |
| 三模态作用程度 | 非强制，可用于分析 | 建议 `(B,3)` |
| 主要参考模态 | 非强制 | B 个模态名 |
| 各模态时序重要性 | 可选 | `(B,T_t)`、`(B,T_a)`、`(B,T_v)` |
| 关键证据 | 非专项硬性输出 | 每样本变长记录，连接文本和秒级/帧级定位 |

回归损失计算前需统一 `(B,)` 与 `(B,1)`，防止广播成 `(B,B)`。输出是每条视频一个片段级预测，不是每个时序位置都必须输出一个有监督情感标签。

## 9 结果文件和提交材料怎么规划

### 9.1 Q1 建议目录

```text
q1_features/
  features/                 # 100条样本的自提取特征
  metadata/                 # 时间映射、有效性、源素材对应
  sample_summary.csv        # 全100条汇总
  extraction_config.json    # 工具版本和参数
  README.md                 # 读取方法和重现命令
```

题目要求全部自生成特征，不能只交挑选过的典型样本。论文正文还要有全量结果汇总和典型样本对齐验证。

### 9.2 Q2 预测 CSV 建议

每个所选版本样本一行，共30行数据：

```csv
sample_id,feature_version,pred_label,pred_score
```

`sample_id` 建议使用去掉扩展名的原文件名；`feature_version` 写 `aligned` 或 `unaligned`；`pred_label` 使用 `Negative/Neutral/Positive`；`pred_score` 是连续预测。若提交数值类别，也要明确0/1/2映射。

可增添三个类别概率列，但不能用概率代替情感强度。附件3没有真值，结果文件不应出现臆造的真实标签或专项测试准确率。

### 9.3 Q3 预测与解释 CSV 建议

每个样本一行，共20行数据：

```csv
sample_id,feature_version,pred_label,pred_score,main_modality,text_weight,audio_weight,vision_weight,evidence_json
```

`evidence_json` 可以存储多个证据对象，例如包含 `modality`、特征位置起止、文本片段、视频时间起止、重要性、定位方式与可用性状态。CSV 中的 JSON 字段需正确转义，可交给标准 CSV 库处理。

明确索引与单位：例如数组位置从0开始、区间采用左闭右开、时间为当前 MP4 片段内的秒数，不能混用原长视频绝对时间。若只得到近似时间映射，应在解释字段中标注，不能伪装成精确时间戳。

正文另外需要典型样本解释卡、主要模态内部片段重要性图、三模态差异分析、全20条结果汇总及验证集评价。建议列名不是官方模板，若之后获得组委会模板，应以最新模板为准。

### 9.4 50 MB 附件限制会影响模型与特征设计

题目要求提交 Q1 全100条特征、Q2/Q3核心代码、说明、模型参数、配置和环境说明，且总附件不超过50 MB。

如果 Q1 使用100条、每条50位置、`768+74+35=877`维、全部 float32，仅三个特征数组的原始大小为：

```text
100 × 50 × 877 × 4 = 17,540,000 字节，约17.54 MB
```

这还不含元数据、两类模型权重和其他材料。采用更长序列、大型微调编码器或保存多个 checkpoint 很容易超出限制；应在定方案时估算，而不是最后才压缩。压缩收益依赖数值内容，不能事先保证。

当前题目没有明确“通用预训练模型权重是否可只附下载说明而免于打包”。若方案依赖体积很大的预训练权重，应确认提交口径，并保留完整版本、配置与获取说明；不能直接假定可以省略题目要求的模型参数文件。原始数据包本身远超50 MB，通常应按输入文件路径引用，重点提交规定的自生成结果和复现材料。

## 10 面向论文的递进方案与验证闭环

本节是方案建议，尚未实施或验证。三问围绕同一个问题展开：如何将不同步、可能缺失的多模态信息转化为可靠且能追溯依据的情感判断。Q1解决“信息怎样表示和对应”，Q2解决“信息不完整时怎样预测”，Q3解决“预测依赖什么、这些依据是否可信”。

### 10.1 三问分别继承什么 新增什么

| 问题 | 继承与基础 | 本问新增工作 | 为下一问或闭环提供的内容 |
| --- | --- | --- | --- |
| Q1 可追溯的多模态表示 | 原视频、转写与片段标识 | 特征提取、时序对齐、有效性掩码、特征位置到原素材的映射 | 统一的序列组织规则；区分填充与不可用的定义；可用于证据回看的映射方法 |
| Q2 缺失感知的鲁棒预测 | Q1的表示与掩码规则；附件2官方特征 | 局部缺失训练、模态可用性处理、时序融合及分类/回归预测 | 一个可复用的预测主干；缺失前后的输出与中间表示；待解释的模态依赖变化 |
| Q3 同一预测过程的可解释分析 | Q2预测主干；Q1的定位与回看方法 | 模态贡献、局部证据、证据干预验证和原素材回溯 | 检验模型是否依赖有效内容、缺失后是否利用其他信息，以及对齐/掩码设计是否合理 |

可以用以下方法流程组织论文总图：

```text
Q1 原信号处理 → 时序表示 X + 有效/观测掩码 M + 原素材映射 τ
                        │ 统一表示规则与处理方法
                        ▼
附件2官方特征 → 适配相同规则 → Q2 鲁棒预测器 fθ(X,M) → 极性与强度
                                         │ 继承预测器及中间表示
                                         ▼
                              Q3 模态贡献 + 局部证据
                                         │
                  ┌──────────────────────┴─────────────────────┐
                  ▼                                            ▼
        原素材回看：复用 Q1 映射方法                 证据干预：复用 Q2 局部遮蔽方法
                  └──────── 检验表示、预测与解释的一致性 ────────┘
```

`τ` 只在有素材且已建立对应关系的样本上存在。附件2/附件3没有提供的映射应标记为未知；附件4需应用并核验 Q1 的映射方法，而不是直接拷贝附件1的时间戳。

### 10.2 数据与模型怎样做到实际衔接

**Q1到Q2：首先统一表示规则，再判断能否共用特征。** 三问使用同一套样本标识习惯、位置索引、填充/观测掩码含义和预处理记录。Q1保存特征及时间映射；附件2通过适配器进入同样的组织形式。只有确认特征语义和处理流程一致，才把 Q1 特征作为已训练预测器的直接输入。不能通过补零、截断或任意投影到74/35维就声称完成了特征空间衔接。

作为第一套贯穿三问的候选方案，可以优先评估对齐版：其音视频和文本均为50个位置，方便统一时序组织；附件3提供 `text_bert`，可以与附件2、附件4采用同一兼容文本编码器。这是基于当前文件接口的规划选择，不是对其预测性能的保证。若最终选择未对齐版，则三问统一采用独立序列及显式时间对应的设计，处理第5节所列长度问题。

**Q2到Q3：优先直接继承预测器。** 可以让 Q3 使用已训练的 Q2 预测主干，在完整输入上增加模态和片段贡献分析；解释模块可以是对固定预测器的归因计算，不一定需要另训一个大模型。这条路线最容易保证“被解释的模型就是实际做预测的模型”。Q2的融合权重可作为待分析量，但还要用输入干预验证，不能直接等同于解释结论。

如果 Q3 为提高解释性进一步训练或改变结构，应从 Q2 参数初始化或明确共享部分，新增参数仍仅在附件2 train 学习、在 valid 选择，并报告相对 Q2 的预测性能变化。此时解释对象是更新后的 Q3 模型，不能把它的贡献图当作原 Q2 决策的直接证明。

**Q3回到Q1/Q2：用解释检验具体设计。** 例如检查高贡献位置是否落在有效观测而非填充上；回看证据区间是否与对应文本、声音或画面一致；在局部遮蔽前后比较输出和其他模态贡献。模型是否发生合理的信息补偿应由实验得出，不能提前假定“缺哪个模态，另两个权重就一定应该升高”。

### 10.3 让闭环有证据的实验安排

| 实验 | 所用数据 | 回答的问题及结论边界 |
| --- | --- | --- |
| 原始素材与对齐结果核验 | 附件1，含题目要求的典型样本 | 特征位置能否回到正确的词、声音和帧段；没有人工时间真值时，以明确的人工核验规则和误差案例报告，不能编造精确对齐准确率 |
| 正常输入与局部缺失对照 | 附件2 train 学习，valid评估；相同样本和扰动设置 | Q2的缺失处理是否改善分类/回归表现，代价是什么 |
| 掩码处理与时序对应消融 | 附件2 train/valid | 比较保留/取消相应模块，或人为错移某模态位置，观察性能变化；错移实验检验对时序对应的依赖，不能据此证明 Q1 自提取器优于官方特征 |
| 关键证据与随机证据删除对照 | 附件2 valid；使用同一预测器 | 在同一模态内删除等数量有效位置，控制片段长度、删除数量及替换方式，比较预测类别得分或回归输出变化，检验解释与决策的联系 |
| 缺失前后贡献变化 | 附件2 valid，同一批样本的完整/受扰动输入 | Q2的鲁棒表现能否得到 Q3 分析支持；使用相同归因尺度，避免仅因归一化权重和为1而误判发生了补偿 |
| 全量专项输出与证据回看 | 附件3输出30条；附件4输出20条并回看 | 在模型和规则选定后完成最终展示；无标签专项数据不能证明准确率提升，也不用于选择模型或阈值 |

Q1的自提取特征与附件2的官方特征并非自动可比。若不能建立兼容关系，论文应将上述实验分别用于证明原始处理的可追溯性、预测的鲁棒性、解释的忠实程度，通过共享方法连接，而不宣称已完成“用Q1全部输出训练Q2”的端到端实验。

形成闭环不要求把专项测试结果反复用于改模型。方法迭代与选择在附件2 train/valid完成；附件4用于最终推理和证据定位核验。若根据 valid 的错误归因修改掩码、融合或解释规则，应重新报告修改后的验证结果，并承认这些是用于开发选择的验证结果。

### 10.4 实施顺序与论文组织

1. **先设计三问共用的数据表示和模型接口。** 明确特征版本、文本编码路线、时序位置与掩码含义，以及哪些样本有可信时间映射。检查附件3/4能否进入这套接口，避免写到 Q2 才发现输入缺字段。
2. **完成 Q1 的100条提取和对齐核验。** 形成特征、时间映射和汇总表，作为论文方法基础；特征维度选择服从提取含义与后续接口的共同需求。
3. **把附件2/3/4适配到共同表示。** 保留三类文件层级差异，统一张量组织，落实视觉长度冲突、全零观测和文本填充的处理规则；不把未知时间戳补成伪精确值。
4. **在共同表示上实现 Q2。** 在附件2 train训练，在 valid进行正常输入、局部缺失和模块消融实验，得到可用于 Q3 的预测器。
5. **继承 Q2 构建 Q3。** 增加贡献与局部证据分析，在 valid完成干预对照；结合 Q1 方法为附件4建立并核验原素材映射。
6. **完成闭环分析，再运行专项预测。** 以同一批验证样本展示“有效信息—缺失扰动—预测变化—贡献变化”，在配置选定后输出附件3/4结果，形成最终典型案例。
7. **按递进逻辑写论文并整理提交。** 总体框架先说明三问联系；Q1章给出表示与时间对应，Q2章在此基础上增加鲁棒预测，Q3章继承预测器并验证证据；结尾汇总各环节支持哪些结论和仍有哪些边界。核查100/30/20条结果覆盖及50 MB限制。

论文中可分别用两句过渡说明递进关系：“在建立可追溯的多模态时序表示后，进一步研究局部观测不可用时的稳定预测”；“在获得鲁棒预测器后，进一步追踪模态与局部片段的决策贡献，并结合原始素材映射核验其依据”。这些过渡必须对应上述实际模块复用与实验，不能只有文字上的连接。

### 10.5 需要明确而不能自行当作既定事实的事项

| 事项 | 已知现状 | 对规划的影响 |
| --- | --- | --- |
| 附件2 `text` 的具体编码器配置 | 题目仅称 BERT 类表示 | 决定能否直接复用与重算混接；否则统一自行编码 |
| 对齐位置与词/子词/秒的关系 | 有对齐概述，无完整时间映射 | Q3精确证据定位需补建和核验 |
| 音频74维和视觉35维的逐维定义 | 当前材料未给完整定义；音频明确不额外假定物理含义 | 可以做向量建模，不宜编造逐维声学/表情名称 |
| 未对齐版视觉长度异常 | 已实测确认 | 不能直接依据 `vision_lengths` 截断 |
| 附件3真实缺失掩码及原有效长度 | 未提供 | 专项人工缺失率不可精确还原，规律分析用有控制的验证实验 |
| 附件4视觉全零 | 对齐版13号已确认 | 要记录模态不可用，不能生成虚假的视觉依据 |
| 官方 CSV 列级模板及 F1 平均方式 | 当前材料未明确 | 自定义时写清口径，后续按正式补充要求调整 |
| 大型预训练权重与50 MB限制 | 未给豁免细则 | 影响是否微调、保存哪些权重与复现方式 |

## 11 核查范围与结论的边界

本次已读取题目正文、两张嵌入图片、两份标签工作簿，并加载附件2两个完整 PKL、附件3全部60个 PKL、附件4全部40个 PKL；检查了字段、形状、类型、标签、划分、零值与长度一致性。还核对附件1视频与标签主键、附件4特征与视频编号，读取视频容器时长并比较附件4两版本的视频内容。

本次没有训练模型，没有推测或恢复专项测试标签，也没有逐帧解码审看全部视频。因此，上述检查不等于确认所有音视频都无解码问题，也不能替代 Q1 正式提取日志与 Q3 证据回看。

题目中的多模态示意图使用一个独立的 CMU-MOSI 示例；它用于说明概念，其中的样本、逐词时间戳、分辨率、帧率和采样率均不能直接套用到本题实际 CMU-MOSEI 文件。
