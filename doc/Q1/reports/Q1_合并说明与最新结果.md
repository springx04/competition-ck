# Q1_new 合并说明与最新结果

## 合并范围

2026-09-26 将 `Q1_new/Q1_ALIGNMENT_REPAIR_STAGE3_WEB_ANALYSIS_20260926` 合入现有 `Q1/q1_features`。Formal 生产运行 `runs/q1_full_20260924` 保持不变；新版的 Stage 2/3 代码、配置、候选运行、逐段来源和验证文件分别保存到 `runs/q1_alignment_repair_stage2/`、`runs/q1_alignment_repair_stage3/`。旧版中仍有价值且与新版相同的文件原样保留，4 个被新版替换的旧脚本/状态文件存放在 `history/pre_stage3_20260925/`，便于追溯。

Stage 3 结果为自动候选而不是生产写回：`candidate_only=true`、`production_write=false`，没有交换 clip、覆盖正式 NPZ 或使用情感标签。新版交接说明和 Stage 4 报告保存在 `docs/repair_history/`；报告记载的 Stage 4 结论没有被当成已存在的本地 Stage 4 NPZ。

## 可复核结果

- Formal：100 条样本，`validation.json` 为 `ok=true`；生产 `paired_use=true/false=49/51`。
- Stage 2：官方文本强制对齐和候选修复证据均保留在 `runs/q1_alignment_repair_stage2/reports/`。
- Stage 3：四指标 mapping 核验、TalkNet speaker anchor 和 ArcFace identity continuity 均只生成候选。100 条状态为 `RESOLVED_VALID=57`、`RESOLVED_PARTIAL=19`、`RESOLVED_CONFLICT=5`、`RESOLVED_MISSING=4`、`UNRESOLVED=15`。
- 37 条原视觉风险的平均可用视觉比例由 Stage 2 的 33.55% 增至 Stage 3 的 51.44%；全100条视觉 observed 为 Formal/Stage2/Stage3=`57.46%/70.10%/76.04%`，trimodal=`39.06%/49.16%/52.14%`。这些是覆盖和候选状态，不是准确率。
- 最大身份连续间隔固定为 1.0 秒；0.5/1.0/1.5 秒的状态计数相同，可用比例为 50.25%/51.44%/51.44%。没有据此宣称参数最优。

## 论文图表

统一图册现有 80 组图，Q1 为 22 组。Q1_11–Q1_22 来自合并后的 Stage 2/3 CSV/NPZ，覆盖：Formal→Stage2→Stage3 覆盖变化、逐样本增量、视觉状态构成与转移、间隔敏感性、mapping 证据、CTC/forced alignment 一致性、阈值敏感性、语义屏蔽、词级 CSR 来源和逐段视觉时间线。图文件按 `doc/paper_figures/figures/Q1/`、`Q2/`、`Q3/` 分目录。

图中 `FULLY_VERIFIED` 只表示满足固定自动规则，不是人工 Gold；`MAPPING_STRONG` 也不允许自动改片；`UNRESOLVED`、`AMBIGUOUS`、自然缺失和冲突 mask 均保留。正式特征与候选特征必须在论文中分开标注。
